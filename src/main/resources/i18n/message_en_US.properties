releaseInfo.name=Call Sub-pipeline
releaseInfo.summary=Call sub-pipelines synchronously/asynchronously
input.project.label=Project ID
input.project.desc=When the project to which the sub-pipeline belongs is passed in as a variable, the value is the English name of the project
input.subPipelineType.label=Sub-pipeline
input.subPipelineType.desc=Select the pipeline to be executed
input.subPipelineType.list[0].label=Select from list
input.subPipelineType.list[1].label=Enter by ID or name
input.subPipelineName.placeholder=Please enter the sub-pipeline ID or name
input.runMode.label=Operation mode
input.runMode.list[0].label=Synchronous
input.runMode.list[1].label=Asynchronous
input.params.label=Sub-pipeline parameters
input.params.desc=The running parameters brought into the sub-pipeline
input.params.param.enableTips=When checked, the value when editing the current plugin and saving the parent pipeline shall prevail, and each execution is passed in from the parent pipeline. <br>When unchecked, the latest setting of the sub-pipeline will always prevail, and will not be passed in from the parent pipeline.
input.paramsStr.label=Sub-pipeline parameters
input.paramsStr.desc=Use = to connect key and value, and separate multiple parameters with newlines
input.fieldNamespace.label=Subpipeline output variable namespace
input.fieldNamespace.desc=Automatically added before the sub-pipeline variable to avoid overwriting the parent pipeline variable, if not filled, the default is sub-pipeline
input.fieldNamespace.placeholder=If not filled, the default is sub_pipeline
input.outputSubBuildVariable.label=Sub-pipeline output variables
input.outputSubBuildVariable.desc=Fill in the name of the sub-pipeline variable that needs to be output, multiple outputs, separated by English commas
input.pollingInterval.label=Polling interval (minutes)
input.pollingInterval.desc=The interval for polling the status of the sub-pipeline, in minutes, supports an integer between 1 and 10
input.pollingInterval.placeholder=If not filled, the default is 10s, and integers between 1 and 10 are supported
input.output.sub_pipeline_buildId.description=Sub-pipeline build ID
input.output.sub_pipeline_url.description=Sub-pipeline URL